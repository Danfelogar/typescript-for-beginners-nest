export const name: string = 'Danfelogar'
export const age: number = 25
export const isValid: boolean = true

export const templateString = `Hola, mi nombre es ${name} y tengo ${age} años e isValid = ${isValid}`